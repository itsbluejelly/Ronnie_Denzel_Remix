name: Test workflows
on: [push, pull_request]
run-name: Tesing env vars
jobs:
  test-workflows:
    environment: Production
    runs-on: ubuntu-latest
    env:
      SUPER_ANTI: "TRUE"
    steps:
        - run: "echo $SUPER_ANTI"
